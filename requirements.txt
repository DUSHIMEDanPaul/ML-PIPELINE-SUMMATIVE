# ---------------------------------------------------------------------------
# HAM10000 skin-lesion classifier — deployment dependencies (pinned)
# Target: Python 3.11 (Docker), CPU-only inference.
#
# The model was trained on Keras 3 / TensorFlow 2.20, so we pin the CPU build
# of the SAME minor version to guarantee the .keras file deserialises identically.
# Use `tensorflow-cpu` (NOT `tensorflow`) — it drops the CUDA/cuDNN payload and
# saves ~2.5 GB in the image.
# ---------------------------------------------------------------------------

# --- Deep learning / model runtime ---
tensorflow-cpu==2.20.0

# --- Numerics / data (versions compatible with TF 2.20's constraints) ---
numpy==2.1.3
pandas==2.2.3
scikit-learn==1.5.2
pillow==11.0.0

# --- API service (FastAPI + ASGI server) ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20       # required for multipart/form-data file uploads
pydantic==2.10.4

# --- UI (Streamlit) + HTTP client it uses to reach the API ---
streamlit==1.41.1
requests==2.32.3

# --- Load testing ---
locust==2.32.6
